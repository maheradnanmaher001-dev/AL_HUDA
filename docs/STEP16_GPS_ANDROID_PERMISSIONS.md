# Step 16 — GPS + Android Permissions

Adds a safe GPS/location foundation, Android coarse/fine runtime permission requests, and the Buildozer permission/dependency update through the workflow. Existing screens are not replaced.
